name: hello-world
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push: 
    branches: [main]

jobs: 
  say-hello:
    runs-on: ubuntu-latest
    steps: 
      - run: echo hello-world 👋🌏

